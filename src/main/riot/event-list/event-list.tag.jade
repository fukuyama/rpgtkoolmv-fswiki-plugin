event-list
  div(each='{ event in events }')
    table
      tr
        td { pad(event.id,3) }
        td { event.name }
        td
          event-position(x='{ event.x }',y='{ event.y }')

  script(src='./event-list.coffee')
