event-page
  event-page-conditions(conditions='{ opts.page.conditions }')
  event-default-move(pages='{ event.pages }')
  event-option(pages='{ event.pages }')
  event-trigger(pages='{ event.pages }')

  script(src='./event-page.coffee')
