event-commands
  div
    span(each='{ command, i in opts.page.list }',riot-tag='code{ command.code }')

  style(scoped,type='less')
    include event-commands.less

  script(type='coffee')
    include event-commands.coffee
