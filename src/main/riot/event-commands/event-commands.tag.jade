event-commands
  div
    ul
      li(
        each='{ command, i in opts.page.list }'
      )
        div code = { command.code }
        div indent = { command.indent }

  style(scoped)
    include event-commands.css

  script(src='./event-commands.coffee')
